# Optional isolated GEPA worker dependency manifest.
#
# This checked-in manifest is intentionally diagnostic-only because
# requirements-gepa.lock does not yet contain reviewer-verified hashes. It must
# not be used to install packages. The plugin never installs dependencies at
# runtime and must never modify Agent Zero's host interpreter.
#
# Once a reviewed hash-complete replacement lock exists, an operator may use it
# only in the dedicated worker venv described in README.md.
-r requirements-gepa.lock
